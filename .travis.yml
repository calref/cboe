
language: cpp

script: scons

compiler:
 - clang

matrix:
 allow_failures:
  - os: linux
 include:
  - os: linux
    sudo: required
    dist: trusty
  - os: osx
    osx_image: xcode7.2

install:
 - ./.travis-install-deps.sh

branches:
 except:
  - legacy-win32
